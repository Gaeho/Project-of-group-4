<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Post">
	
	<select id="postSelect" resultType="Post">
		select *
		from post
		<trim prefix="WHERE" prefixOverrides="AND|OR">
			<if test="searchType=='post_title' and keyword != null and keyword != '' ">
				AND post_title like CONCAT('%', #{keyword}, '%')
			</if>
			<if test="searchType=='post_ctx' and keyword != null and keyword != '' ">
				AND post_ctx like CONCAT('%', #{keyword}, '%')
			</if>
			<if test="searchType=='user_id' and keyword != null and keyword != '' ">
				AND user_id like CONCAT('%', #{keyword}, '%')
			</if>	
		</trim>
		order by post_code desc, post_regdate desc
	</select>
	
	<select id="postSelectOne" parameterType="java.lang.String" resultType="Post">
		select * 
		from post		
		where post_code = #{post_code}
	</select>
	
	<select id="postTotal" resultType="int">
		select *
		from post
		<trim prefix="WHERE" prefixOverrides="AND|OR">
		<if test="keyword != null and keyword != '' ">
			<if test="searchType=='post_title'">
				and post_title like CONCAT('%', #{keyword}, '%')
			</if>
			<if test="searchType=='post_ctx'">
				and post_ctx like CONCAT('%', #{keyword}, '%')
			</if>
			<if test="searchType=='user_id'">
				and user_id like CONCAT('%', #{keyword}, '%')
			</if>
     	 </if>
		</trim>
	</select>
		
	<insert id="postInsert">
		insert into post(
			post_title
			, post_ctx
			, post_regdate
			, post_view
			, user_id
		) values (
			#{post_title}
			, #{post_ctx}
			, #{post_regdate}
			, #{post_view}
			, #{user_id}
		)
	</insert>
	
	<select id="postView" parameterType="int" resultType="Post">
		select 
			post_code
			, post_title
			, post_ctx
			, post_regdate
			, post_view
		from post
		where post_code = #{post_code}
	</select>
	
	<update id="postUpdate">
		update post
		set
			post_title = #{post_title}
			, post_ctx = #{post_ctx}
		where 
			post_code = #{post_code} 
	</update>
	
	<delete id="postDelete">
		delete
		from post
		where user_id = #{user_id}
	</delete>
	
    
    <insert id="postReply">
    	insert into post (post_code, ref, ref_step, ref_ctx)
    	values (#{post_code}, #{ref}, #{ref_step}, #{ref_ctx})
    </insert>

	<select id="postGetReply" resultType="Post">
		select ref, post_code as post_code, ref_step as re ref_step, ref_ctx
		from post
		where post_code = #{post_code}
		order by ref_step asc
	</select>
</mapper>